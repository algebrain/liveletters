{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the LiveLetters window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "default"
  ]
}
